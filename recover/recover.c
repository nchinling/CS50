#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
 // Check command-line arguments. Only 1
    if (argc != 2 || )
    {
        printf("Usage: ./recover filename.c\n");
        return 1;
    }
}